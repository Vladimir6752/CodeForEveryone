package dev.vladimir.cfecodemodule.tokens.another;

import dev.vladimir.cfecodemodule.tokens.Token;

public class IfToken extends Token {
    public IfToken() {
        super(new String[] { "Если" }, "if", "Если");
    }
}
